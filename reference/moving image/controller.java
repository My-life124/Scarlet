package tutorial;
/* package tutorial;

import java.util.LinkedList;
import java.awt.Graphics;

public class controller {
    private LinkedList<Enemy> e = new LinkedList<Enemy>();

Enemy tempEnemy;


private Game game;
private texture tx;
public controller(Game game, texture tx) {
this.game = game;
for (int x= 0;  x < Game.WIDTH * Game.SCALE; x+=64) {
    addEnemy(new Enemy(x, 0 , tx));
    }
}
 public void tick() {
for (int i = 0; i < e.size(); i++) {
	tempEnemy = e.get(i);
	if (tempEnemy.getY()< 0) {
	removeEnemy(tempEnemy);
}
tempEnemy.tick();
}
}

public void render(Graphics g) {
for (int i = 0; i < e.size(); i++) {
tempEnemy = e.get(i);
tempEnemy.render(g);
}
}
private void removeEnemy(Enemy block) {
    e.remove(block);
}
private void addEnemy(Enemy block) {
    e.add(block);

}
}
*/
